# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Shopify-Admin' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Shopify-Admin
  pod 'Kingfisher', '~> 7.0'
  pod 'Alamofire'
  pod 'Reachability', '~> 3.2'
  pod 'Cosmos', '~> 23.0'
  pod 'SwiftyGif'
  pod 'Floaty', '~> 4.1.0'
  target 'Shopify-AdminTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Shopify-AdminUITests' do
    # Pods for testing
  end

end
